# Sample config file for ConfigRunner. Run with:
#   ConfigRunner.run(Paths.get("path/to/sample.conf"))
# or: java -cp ... com.cstesting.config.ConfigRunner path/to/sample.conf
#
# Commands: goto, type, click, doubleClick, rightClick, hover, wait, screenshot,
#           frame, dialogAccept, dialogAcceptPrompt, dialogDismiss, check, uncheck, select,
#           verifyText, assertAttribute, assertTextEqualsAttribute, close

goto https://example.com
wait 1000
verifyText h1 "Example Domain"
click "a"
wait 2000
# screenshot target/screenshot.png
# close
